{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run",
            "type": "shell",
            "command": "go build && ./inf273",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}